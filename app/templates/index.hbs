{{#if model.length}}
	<section class="main">
		<ul class="todo-list">
			{{#each model as |todo|}}
				<li>
					<input class="toggle" type="checkbox">
					<label>{{todo.title}}</label>
					<button class="destroy"></button>
				</li>
			{{/each}}
		</ul>
	</section>
{{/if}}
